package com.api.books;

import org.springframework.stereotype.Service;

@Service
public class BookService {

    /*
     * private List<Book> books = new ArrayList<Book>();
     * 
     * public List<Book> getAllBooks() { books.add(new Book("1", "Book1", "A",
     * "First Book")); books.add(new Book("2", "Book2", "B", "Second Book")); return
     * books; }
     * 
     * public Book getBook(String id) { return books.stream().filter(t ->
     * t.getId().equals(id)).findFirst().get(); }
     * 
     * public void addTopic(Book book) { books.add(book); }
     */
}
